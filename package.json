{
  "name": "patchlight",
  "version": "0.2.0",
  "private": true,
  "description": "An independent, no-hardware block-wiring playground. No runtime dependencies or build step.",
  "license": "MIT",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "test:all": "npm test && npm run test:browser"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
